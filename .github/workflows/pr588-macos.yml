# SPDX-License-Identifier: AGPL-3.0-only
# PR #588 (unslothai/unsloth-zoo) cross-platform CI on macOS runners.
# Confirms unsloth + PR-patched unsloth_zoo imports cleanly on macos-14
# (Apple Silicon) via the CUDA spoof harness, that `unsloth studio --help`
# still works, and that the PR's sim test suite passes on this OS.

name: PR588 macOS
on:
  push:
    branches: [pr-588-cross-platform-ci]
    paths:
      - 'tests/pr588/**'
      - '.github/workflows/pr588-macos.yml'
      - 'tests/conftest.py'
      - 'tests/_zoo_aggressive_cuda_spoof.py'
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  smoke:
    runs-on: macos-14
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install CPU torch + deps
        run: |
          python -m pip install --upgrade pip
          python -m pip install torch
          python -m pip install transformers pytest accelerate peft trl datasets Pillow torchvision
          # bitsandbytes + triton wheels are Linux-only; the PR's sim tests
          # that touch bnb/triton code only inspect source (no real call).
          # Install a minimal triton-stub so the unsloth_zoo top-level
          # imports succeed.
      - name: Install triton stub (macOS has no official triton)
        run: |
          STUB="$RUNNER_TEMP/triton_stub"
          python - <<PY
          import os, pathlib
          root = pathlib.Path(os.environ["RUNNER_TEMP"]) / "triton_stub"
          (root / "triton").mkdir(parents=True, exist_ok=True)
          (root / "triton" / "language").mkdir(exist_ok=True)
          (root / "triton" / "runtime").mkdir(exist_ok=True)
          (root / "triton" / "compiler").mkdir(exist_ok=True)
          (root / "triton" / "backends").mkdir(exist_ok=True)
          (root / "triton" / "__init__.py").write_text(
              '__version__ = "3.5.0+macos-stub"\n'
              'def jit(fn=None, *a, **k):\n'
              '    return fn if fn is not None else (lambda f: f)\n'
              'def autotune(*a, **k):\n'
              '    return lambda fn: fn\n'
              'def heuristics(*a, **k):\n'
              '    return lambda fn: fn\n'
              'def Config(*a, **k):\n'
              '    return None\n'
              'def next_power_of_2(x):\n'
              '    return 1 if x <= 1 else 1 << (int(x) - 1).bit_length()\n'
              'from . import language, runtime, compiler, backends\n'
          )
          (root / "triton" / "backends" / "__init__.py").write_text(
              'def backends():\n'
              '    return {}\n'
          )
          # torch._inductor.runtime.hints does `import triton.backends.compiler`
          # at top-level on torch>=2.5; satisfy it with a minimal stub.
          (root / "triton" / "backends" / "compiler.py").write_text(
              'class BaseBackend:\n'
              '    def __init__(self, *a, **k): pass\n'
              'class GPUTarget:\n'
              '    def __init__(self, *a, **k): pass\n'
              'class AttrsDescriptor:\n'
              '    def __init__(self, *a, **k): pass\n'
          )
          (root / "triton" / "backends" / "driver.py").write_text(
              'class DriverBase:\n'
              '    def __init__(self, *a, **k): pass\n'
          )
          (root / "triton" / "language" / "__init__.py").write_text(
              'class dtype:\n'
              '    def __init__(self, *a, **k): pass\n'
              'class constexpr:\n'
              '    def __init__(self, *a, **k): pass\n'
              'float16 = dtype()\n'
              'float32 = dtype()\n'
              'bfloat16 = dtype()\n'
              'int8 = dtype()\n'
              'int16 = dtype()\n'
              'int32 = dtype()\n'
              'int64 = dtype()\n'
              'pointer_type = dtype\n'
              'def program_id(*a, **k): return 0\n'
              'def arange(*a, **k): return None\n'
              'def load(*a, **k): return None\n'
              'def store(*a, **k): return None\n'
              'from . import extra\n'
              # PEP 562 module-level __getattr__: catch any other names
              # unsloth.kernels.utils grabs off triton.language (cast, etc.)
              # and resolve them to no-op callables.
              'def __getattr__(name):\n'
              '    return lambda *a, **k: None\n'
          )
          (root / "triton" / "language" / "extra").mkdir(exist_ok=True)
          # unsloth.kernels.utils does `from triton.language.extra import libdevice`.
          # libdevice wraps CUDA math intrinsics; stub via __getattr__ so any name
          # resolves to a no-op callable.
          (root / "triton" / "language" / "extra" / "__init__.py").write_text(
              'class _LibdeviceStub:\n'
              '    def __getattr__(self, name):\n'
              '        return lambda *a, **k: None\n'
              'libdevice = _LibdeviceStub()\n'
          )
          # Also stub bitsandbytes on macOS (Linux-only wheel; we only need
          # imports to succeed for the Studio CLI smoke).
          (root / "bitsandbytes").mkdir(exist_ok=True)
          (root / "bitsandbytes" / "__init__.py").write_text(
              '__version__ = "0.49.2+macos-stub"\n'
              'class Linear4bit:\n'
              '    def __init__(self, *a, **k): pass\n'
              'class Linear8bitLt:\n'
              '    def __init__(self, *a, **k): pass\n'
              # Expose functional as attribute so `bnb.functional.get_ptr`
              # works (unsloth.kernels.utils uses that pattern).
              'from . import functional\n'
          )
          (root / "bitsandbytes" / "functional.py").write_text(
              'def dequantize_4bit(*a, **k): return None\n'
              'def quantize_4bit(*a, **k): return None\n'
              'def get_ptr(*a, **k): return None\n'
              'class QuantState:\n'
              '    def __init__(self, *a, **k): pass\n'
              '    def as_dict(self, packed=True): return {}\n'
              # unsloth.kernels.utils grabs C-bridge funcs off bnb.functional.lib;
              # stub via __getattr__ so any name resolves to a no-op.
              'class _LibStub:\n'
              '    def __getattr__(self, name):\n'
              '        return lambda *a, **k: None\n'
              'lib = _LibStub()\n'
          )
          (root / "bitsandbytes" / "nn").mkdir(exist_ok=True)
          (root / "bitsandbytes" / "nn" / "__init__.py").write_text(
              'from ..__init__ import Linear4bit, Linear8bitLt\n'
              'class Params4bit:\n'
              '    def __init__(self, *a, **k): pass\n'
              'from . import modules\n'
          )
          # zoo's vllm_utils does `import bitsandbytes.nn.modules`.
          (root / "bitsandbytes" / "nn" / "modules.py").write_text(
              'from ..__init__ import Linear4bit, Linear8bitLt\n'
              'from . import Params4bit\n'
              'class Int8Params:\n'
              '    def __init__(self, *a, **k): pass\n'
          )
          # unsloth_zoo.vllm_utils does `from bitsandbytes.utils import
          # pack_dict_to_tensor, unpack_tensor_to_dict` at import time.
          (root / "bitsandbytes" / "utils.py").write_text(
              'def pack_dict_to_tensor(d): return None\n'
              'def unpack_tensor_to_dict(t): return {}\n'
          )
          (root / "triton" / "runtime" / "__init__.py").write_text('')
          (root / "triton" / "runtime" / "autotuner.py").write_text(
              'class Autotuner: pass\n'
          )
          # zoo/torch import `from triton.compiler import CompiledKernel`
          # AND unsloth/import_fixes.py does `triton.compiler.compiler.CompiledKernel`,
          # so expose CompiledKernel on both surfaces.
          (root / "triton" / "compiler" / "__init__.py").write_text(
              'from .compiler import CompiledKernel, AttrsDescriptor\n'
          )
          (root / "triton" / "compiler" / "compiler.py").write_text(
              'class AttrsDescriptor:\n'
              '    def __init__(self, *a, **k): pass\n'
              'class CompiledKernel:\n'
              '    launch_enter_hook = None\n'
              '    launch_exit_hook = None\n'
              '    def __init__(self, *a, **k): pass\n'
          )
          print("stub written to", root)
          PY
          echo "PYTHONPATH=$STUB:${PYTHONPATH:-}" >> "$GITHUB_ENV"
          PYTHONPATH="$STUB" python -c "import triton; import triton.language; from triton.compiler.compiler import AttrsDescriptor; print('triton stub:', triton.__version__)"

      - name: Install unsloth_zoo @ main + overlay PR-588 4-file diff
        run: |
          # Install zoo from unslothai main so the latest unsloth API surface
          # (e.g. _unsloth_get_mm_token_id) is present. The PR-588 head fork
          # is too old to satisfy current unsloth main.
          python -m pip install --no-deps "unsloth_zoo @ git+https://github.com/unslothai/unsloth-zoo.git@main"
          # Resolve site-packages WITHOUT importing unsloth_zoo (its top-level
          # init crashes on a GPU-less runner unless the CUDA spoof is loaded).
          SITE=$(python -c "import sysconfig; print(sysconfig.get_paths()['purelib'])")
          ZOO_DIR="$SITE/unsloth_zoo"
          for f in empty_model.py hf_utils.py vllm_utils.py; do
            curl -sL "https://raw.githubusercontent.com/Datta0/unsloth-zoo/qwen35-shared-final/unsloth_zoo/$f" -o "$ZOO_DIR/$f"
          done
          echo "Overlay complete -- PR-588's 3 modified files now in $ZOO_DIR"
          ls -la "$ZOO_DIR/empty_model.py" "$ZOO_DIR/hf_utils.py" "$ZOO_DIR/vllm_utils.py"

      - name: Install unsloth from this staging branch
        run: python -m pip install --no-deps -e .

      - name: Apply CUDA spoof + import unsloth
        run: |
          python - <<'PY'
          import sys, importlib.util
          spec = importlib.util.spec_from_file_location("_pr588_conftest", "tests/conftest.py")
          mod = importlib.util.module_from_spec(spec)
          spec.loader.exec_module(mod)
          sys.path.insert(0, "tests")
          import _zoo_aggressive_cuda_spoof
          _zoo_aggressive_cuda_spoof.apply()
          # macOS-specific: zoo's temporary_patches/gemma3n.py applies
          # @torch.compile at module import time, which on macOS pulls in
          # the full torch._inductor chain that ultimately demands a real
          # triton (CompiledKernel etc.). Stub out torch.compile so the
          # decorator becomes a no-op and the inductor chain stays cold.
          import torch
          torch.compile = lambda fn=None, *a, **k: (fn if fn is not None else (lambda f: f))
          import unsloth_zoo
          print("unsloth_zoo:", unsloth_zoo.__version__)
          from unsloth_zoo.empty_model import (
              _is_gemma4_config, extract_gdn_layers, finalize_huggingface_model,
              patch_gemma4_vllm_lora_support, patch_gemma4_vllm_k_eq_v_support,
              get_model_layer_config,
          )
          from unsloth_zoo.vllm_utils import assert_same_state_dict
          from unsloth_zoo.hf_utils import set_dtype_in_config, dtype_from_config
          print("PR-588 symbols import OK on macOS")
          PY

      - name: Confirm `unsloth studio --help` still works
        run: |
          python - <<'PY'
          import sys, importlib.util
          spec = importlib.util.spec_from_file_location("_pr588_conftest", "tests/conftest.py")
          mod = importlib.util.module_from_spec(spec)
          spec.loader.exec_module(mod)
          sys.path.insert(0, "tests")
          import _zoo_aggressive_cuda_spoof
          _zoo_aggressive_cuda_spoof.apply()
          import torch
          torch.compile = lambda fn=None, *a, **k: (fn if fn is not None else (lambda f: f))
          import unsloth
          print("unsloth imported OK on macOS")
          PY
          unsloth studio --help 2>&1 | head -40 || echo "studio CLI not on PATH (import smoke ok)"

      - name: Run PR-588 sim test suite
        env:
          PYTHONPATH: tests
        run: |
          # Skip sim_extract_gdn_fp8 and merger_fc_quant_regression: both touch
          # bnb-shaped tensors that need bitsandbytes (Linux-only).
          python -m pytest tests/pr588 -v --tb=short \
            -o python_files="sim_*.py test_*.py" \
            --deselect tests/pr588/sim_extract_gdn.py::test_non_square_dimensions \
            -k "not merger_fc"
