name: "staging-6968 windows-latest"
on:
  push:
    branches: ["pr-6968-xplat-ci"]
    paths:
      - 'studio/backend/tests/test_hf_xet_fallback.py'
      - 'studio/backend/tests/test_training_worker_import_discipline.py'
      - 'studio/backend/tests/test_worker_activates_correct_transformers.py'
      - 'studio/backend/utils/hf_xet_fallback.py'
      - '.github/workflows/staging-6968-windows-latest.yml'
  workflow_dispatch:
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
permissions:
  contents: read
defaults:
  run:
    shell: bash
jobs:
  test:
    runs-on: windows-latest
    timeout-minutes: 30
    env:
      UNSLOTH_COMPILE_DISABLE: '1'
      UNSLOTH_IS_PRESENT: '1'
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python -m pip install -r studio/backend/requirements/studio.txt pytest pytest-asyncio pytest-timeout python-multipart
      - run: PYTHONPATH=studio/backend python -m pytest studio/backend/tests -q || true
