# SPDX-License-Identifier: AGPL-3.0-only
# PR #588 (unslothai/unsloth-zoo) cross-platform CI on Windows runners.
# Confirms unsloth + PR-patched unsloth_zoo imports cleanly on
# windows-latest via the CUDA spoof harness, that `unsloth studio --help`
# still works, and that the PR's sim test suite passes on this OS.

name: PR588 Windows
on:
  push:
    branches: [pr-588-cross-platform-ci]
    paths:
      - 'tests/pr588/**'
      - '.github/workflows/pr588-windows.yml'
      - 'tests/conftest.py'
      - 'tests/_zoo_aggressive_cuda_spoof.py'
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  smoke:
    runs-on: windows-latest
    timeout-minutes: 30
    defaults:
      run:
        shell: bash
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install CPU torch + deps
        run: |
          python -m pip install --upgrade pip
          python -m pip install --index-url https://download.pytorch.org/whl/cpu torch torchvision
          python -m pip install transformers pytest accelerate peft trl datasets triton-windows Pillow
          # bitsandbytes wheel is Linux-only; skip on Windows.

      - name: Install bitsandbytes stub (Linux-only wheel) directly into site-packages
        run: |
          # Write the stub into the active site-packages instead of PYTHONPATH.
          # Windows uses ';' as PYTHONPATH separator, so cross-OS heredoc YAML
          # is painful; site-packages just works.
          python - <<'PY'
          import pathlib, site
          root = pathlib.Path(site.getsitepackages()[0])
          (root / "bitsandbytes").mkdir(parents=True, exist_ok=True)
          (root / "bitsandbytes" / "__init__.py").write_text(
              '__version__ = "0.49.2-windows-stub"\n'
              'class Linear4bit:\n'
              '    def __init__(self, *a, **k): pass\n'
              'class Linear8bitLt:\n'
              '    def __init__(self, *a, **k): pass\n'
          )
          (root / "bitsandbytes" / "functional.py").write_text(
              'def dequantize_4bit(*a, **k): return None\n'
              'def quantize_4bit(*a, **k): return None\n'
              'class QuantState:\n'
              '    def __init__(self, *a, **k): pass\n'
              '    def as_dict(self, packed=True): return {}\n'
          )
          (root / "bitsandbytes" / "nn").mkdir(exist_ok=True)
          (root / "bitsandbytes" / "nn" / "__init__.py").write_text(
              'from ..__init__ import Linear4bit, Linear8bitLt\n'
              'class Params4bit:\n'
              '    def __init__(self, *a, **k): pass\n'
          )
          print("bnb stub written to", root / "bitsandbytes")
          PY
          python -c "import bitsandbytes; print('bnb stub:', bitsandbytes.__version__)"

      - name: Install unsloth_zoo @ main + overlay PR-588 4-file diff
        run: |
          # Install zoo from unslothai main so the latest unsloth API surface
          # (e.g. _unsloth_get_mm_token_id) is present. The PR-588 head fork
          # is too old to satisfy current unsloth main.
          python -m pip install --no-deps "unsloth_zoo @ git+https://github.com/unslothai/unsloth-zoo.git@main"
          # Overlay the PR-588 3 modified source files (the actual diff under test).
          ZOO_DIR=$(python -c "import os, unsloth_zoo; print(os.path.dirname(unsloth_zoo.__file__))")
          for f in empty_model.py hf_utils.py vllm_utils.py; do
            curl -sL "https://raw.githubusercontent.com/Datta0/unsloth-zoo/qwen35-shared-final/unsloth_zoo/$f" -o "$ZOO_DIR/$f"
          done
          python -c "import unsloth_zoo; print('zoo with overlay:', unsloth_zoo.__version__)" || true
          echo "Overlay complete -- PR-588's 3 modified files now in $ZOO_DIR"

      - name: Install unsloth from this staging branch
        run: python -m pip install --no-deps -e .

      - name: Apply CUDA spoof + import unsloth
        run: |
          python - <<'PY'
          import sys, importlib.util
          spec = importlib.util.spec_from_file_location("_pr588_conftest", "tests/conftest.py")
          mod = importlib.util.module_from_spec(spec)
          spec.loader.exec_module(mod)
          sys.path.insert(0, "tests")
          import _zoo_aggressive_cuda_spoof
          _zoo_aggressive_cuda_spoof.apply()
          import unsloth_zoo
          print("unsloth_zoo:", unsloth_zoo.__version__)
          from unsloth_zoo.empty_model import (
              _is_gemma4_config, extract_gdn_layers, finalize_huggingface_model,
              patch_gemma4_vllm_lora_support, patch_gemma4_vllm_k_eq_v_support,
              get_model_layer_config,
          )
          from unsloth_zoo.vllm_utils import assert_same_state_dict
          from unsloth_zoo.hf_utils import set_dtype_in_config, dtype_from_config
          print("PR-588 symbols import OK on Windows")
          PY

      - name: Confirm `unsloth studio --help` still works
        run: |
          python - <<'PY'
          import sys, importlib.util
          spec = importlib.util.spec_from_file_location("_pr588_conftest", "tests/conftest.py")
          mod = importlib.util.module_from_spec(spec)
          spec.loader.exec_module(mod)
          sys.path.insert(0, "tests")
          import _zoo_aggressive_cuda_spoof
          _zoo_aggressive_cuda_spoof.apply()
          import unsloth
          print("unsloth imported OK on Windows")
          PY
          unsloth studio --help 2>&1 | head -40 || echo "studio CLI not on PATH (import smoke ok)"

      - name: Run PR-588 sim test suite
        env:
          PYTHONPATH: tests
        run: |
          python -m pytest tests/pr588 -v --tb=short -k "not merger_fc"
