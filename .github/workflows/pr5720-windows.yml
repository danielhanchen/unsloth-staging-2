name: PR5720 windows

on:
  push:
    branches: [pr5720-cross-os]
    paths:
      - 'studio/backend/**'
      - 'studio/desktop/**'
      - '.github/workflows/pr5720-windows.yml'
  pull_request:
    branches: [main]
    paths:
      - 'studio/backend/**'
      - '.github/workflows/pr5720-windows.yml'
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  pytest:
    runs-on: windows-latest
    timeout-minutes: 30
    env:
      PYTHONPATH: ${{ github.workspace }}\studio
      UNSLOTH_COMPILE_DISABLE: '1'
      UNSLOTH_IS_PRESENT: '1'
      PROTOCOL_BUFFERS_PYTHON_IMPLEMENTATION: python
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'

      - name: Install backend test deps
        shell: bash
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r studio/backend/requirements/studio.txt
          python -m pip install 'pytest==9.0.3' pytest-asyncio pytest-timeout python-multipart

      - name: Show env
        shell: bash
        run: |
          python -V
          python -c "import sys; print(sys.platform)"
          pip list 2>/dev/null | head -40

      - name: Run Gemini provider backend tests
        shell: bash
        run: |
          python -m pytest -v --tb=short --timeout=60 \
            studio/backend/tests/test_gemini_provider.py \
            studio/backend/tests/test_external_provider_usage_chunk.py \
            studio/backend/tests/test_providers_api.py
