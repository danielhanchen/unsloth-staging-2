# SPDX-License-Identifier: AGPL-3.0-only
# Copyright 2026-present the Unsloth AI Inc. team. All rights reserved.
#
# MLX vs HF parity bisection probes -- one Mac M1 job per probe.
#
# Why parallel matrix: each probe is fully independent (different
# subprocess, different pip state matters not at all because the
# install layer is the same for each). Fanning out lets:
#   * a single failing probe NOT block the diagnostic data from the
#     remaining probes (already had continue-on-error, but matrix gives
#     each its own job log + artifact + duration);
#   * total wall-time = max(probe_install + probe_run) instead of
#     sum across probes;
#   * future probes added without touching the existing ones.
#
# Each probe job:
#   1. installs the common dep set (MLX + torch CPU + transformers + zoo)
#   2. runs ONE probe
#   3. always uploads its probe_${N}.json as an artifact
#
# A final aggregate job downloads all artifacts and prints a single
# summary table to its log so the human reader can see all probes
# without clicking into 10 separate job logs.

name: MLX parity probes

on:
  pull_request:
    paths:
      - 'tests/mlx_parity/**'
      - '.github/workflows/mlx-parity-probe.yml'
  push:
    branches: [mlx-parity-probes]
  workflow_dispatch: {}

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  probe:
    name: probe-${{ matrix.id }}
    runs-on: macos-14
    timeout-minutes: 25
    strategy:
      fail-fast: false
      matrix:
        include:
          # Mac runners are capped at 5 parallel jobs on the free tier.
          # Keep the matrix lean: only probes that produce NEW information
          # we have not already pinned via earlier runs. Other probe
          # scripts remain on disk for ad-hoc reruns.
          - id: '10'
            script: probe_10_hf_curve_control.py
          - id: '11'
            script: probe_11_mlx_fp32_curve.py
          - id: '12'
            script: probe_12_zoo_prev634.py
            zoo_pin: 'f37d510'
          - id: '13'
            script: probe_13_pure_mlx_inference.py
    steps:
      - name: Harden runner (audit)
        uses: step-security/harden-runner@a5ad31d6a139d249332a2605b85202e8c0b78450  # v2.19.1
        with:
          egress-policy: audit

      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2
        with:
          persist-credentials: false

      - uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405  # v6.2.0
        with:
          python-version: '3.12'
          cache: 'pip'

      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          pip install \
            'mlx==0.30.0' \
            'mlx-lm==0.30.0' \
            'numpy==2.4.4' \
            'pytest==9.0.3'
          pip install --index-url https://download.pytorch.org/whl/cpu \
            'torch==2.10.0'
          pip install \
            'transformers==4.57.6' \
            'peft==0.18.0' \
            'datasets==4.3.0' \
            'accelerate==1.13.0' \
            'sentencepiece==0.2.1' \
            'huggingface-hub==0.36.2' \
            'trl==0.27.0'
          ZOO_REF='${{ matrix.zoo_pin }}'
          if [ -z "$ZOO_REF" ]; then
            ZOO_SPEC='unsloth_zoo @ git+https://github.com/unslothai/unsloth-zoo'
          else
            ZOO_SPEC="unsloth_zoo @ git+https://github.com/unslothai/unsloth-zoo@${ZOO_REF}"
          fi
          for attempt in 1 2 3; do
            if pip install "$ZOO_SPEC"; then break; fi
            if [ "$attempt" -eq 3 ]; then exit 1; fi
            sleep $((5*attempt))
          done

      - name: Run probe ${{ matrix.id }} — ${{ matrix.script }}
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
          UNSLOTH_COMPILE_DISABLE: '1'
        run: |
          cd tests/mlx_parity && python ${{ matrix.script }}

      - name: Show JSON output
        if: always()
        run: |
          F=tests/mlx_parity/.out/probe_${{ matrix.id }}.json
          echo "=== ${F} ==="
          cat "${F}" 2>/dev/null || echo "(no JSON written)"

      - name: Upload probe artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: probe-${{ matrix.id }}
          path: tests/mlx_parity/.out/probe_${{ matrix.id }}.json
          if-no-files-found: warn

  aggregate:
    name: aggregate
    needs: probe
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: Download all probe artifacts
        uses: actions/download-artifact@v4
        with:
          path: probes
          pattern: probe-*
          merge-multiple: true

      - name: Summary
        run: |
          echo "=== probe artifacts ==="
          ls -la probes/ || true
          echo
          for n in 10 11 12 13; do
            echo "--- probe_${n}.json ---"
            cat probes/probe_${n}.json 2>/dev/null || echo "(missing -- probe ${n} job did not produce output)"
            echo
          done
